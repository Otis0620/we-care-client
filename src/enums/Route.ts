export enum RouteName {
  Dashboard = 'dashboard',
  Patients = 'patients',
  Appointments = 'appointments',
  Reports = 'reports',
  Departments = 'departments',
  Payments = 'payments',
  Settings = 'settings',
  Help = 'help',
  Products = 'products',
}

export enum RoutePath {
  Dashboard = '/dashboard',
  Patients = '/patients',
  Appointments = '/appointments',
  Reports = '/reports',
  Departments = '/departments',
  Payments = '/payments',
  Settings = '/settings',
  Help = '/help',
  Products = '/products',
}
